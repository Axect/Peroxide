matrix:
  include:
    - language: rust
      rust:
        - stable
        - beta
        - nightly
      script:
        - cargo build --verbose --all
        - cargo test --verbose --all
      fast_finish: true

    - language: python
      python:
        - "3.7"
      install:
        - pip install matplotlib